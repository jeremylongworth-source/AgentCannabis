interface:
  display_name: "Review Sample Integrity"
  short_description: "Review sample integrity for completeness, traceability, conflicts, and required human follow-up without changing the..."
  default_prompt: "Use $review-sample-integrity to prepare a bounded AgentCannabis review with evidence gaps, source-currentness limits, and responsible human decisions clearly separated."
