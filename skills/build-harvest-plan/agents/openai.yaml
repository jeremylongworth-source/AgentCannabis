interface:
  display_name: "Build Harvest Plan"
  short_description: "Build an evidence-backed harvest plan from existing approved records, criteria, and reviewer assignments"
  default_prompt: "Use $build-harvest-plan to prepare a bounded AgentCannabis review with evidence gaps, source-currentness limits, and responsible human decisions clearly separated."
