interface:
  display_name: "Identify Critical Control Point"
  short_description: "Identify critical control point using supplied evidence, current-source checks, and the repository's review-only auth..."
  default_prompt: "Use $identify-critical-control-point to prepare a bounded AgentCannabis review with evidence gaps, source-currentness limits, and responsible human decisions clearly separated."
