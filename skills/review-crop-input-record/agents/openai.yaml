interface:
  display_name: "Review Crop Input Record"
  short_description: "Review crop input record for completeness, traceability, conflicts, and required human follow-up without changing the..."
  default_prompt: "Use $review-crop-input-record to prepare a bounded AgentCannabis review with evidence gaps, source-currentness limits, and responsible human decisions clearly separated."
