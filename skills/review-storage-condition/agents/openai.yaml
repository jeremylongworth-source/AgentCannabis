interface:
  display_name: "Review Storage Condition"
  short_description: "Review storage condition for completeness, traceability, conflicts, and required human follow-up without changing the..."
  default_prompt: "Use $review-storage-condition to prepare a bounded AgentCannabis review with evidence gaps, source-currentness limits, and responsible human decisions clearly separated."
