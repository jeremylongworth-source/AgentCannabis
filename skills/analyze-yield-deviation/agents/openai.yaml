interface:
  display_name: "Analyze Yield Deviation"
  short_description: "Analyze yield deviation from supplied records, preserving original observations, units, uncertainty, and conflicting..."
  default_prompt: "Use $analyze-yield-deviation to prepare a bounded AgentCannabis review with evidence gaps, source-currentness limits, and responsible human decisions clearly separated."
